name: web_document_scanner
description: "Web Document Scanner"
version: 0.0.1
homepage: https://resam.site
repository: https://github.com/reesaam/web_document_scanner.git
funding:
  - https://resam.site

platforms:
  web:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.2+1
  image: ^4.5.4
  permission_handler: ^12.0.1
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
   assets:
     - assets/

